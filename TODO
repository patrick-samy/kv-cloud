- Add key size check in the API.
- Handle response when calling set with an already present key.
- Add multithreading support for the server (client queue + pool).
- Implement the optional timeout.
- Implement memory caching using an in-memory representation of the B+tree based FS
- Add a configuration file for the server.
- Handle multiple requests in one connection by adding a keep-alive flag.
- Distribute storage by allowing the API to request on multiple servers.

